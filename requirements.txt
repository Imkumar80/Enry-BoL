# Enry-BoL Dependencies
fastapi
uvicorn
pydantic
python-dotenv
python-multipart

# LangGraph + LLMs
langgraph
langchain-ollama
langchain-core
langchain-google-genai

# Voice Pipeline
webrtcvad-wheels
deepgram-sdk
httpx

# Legacy NLU Parser (Gemini fallback)
# nlu_parser.py uses the new `google-genai` SDK (`from google import genai`),
# NOT the older `google-generativeai` package — installing the latter causes
# every /api/parse-command call to crash with:
#   ImportError: cannot import name 'genai' from 'google'
google-genai

# Optional: Local ASR fallback
# faster-whisper